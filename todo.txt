1. ✅ Select range: In the "Add faces to a name" tab, after selecting a name, it should be possible to select a range of the "Most similar unnamed faces". Maybe by holding SHIFT and selecting the first and the last image.

2. ✅ Export feature: In the "Add faces to a name" tab, after selecting a name, it should be possible to export all images of the selected person. The user should be able to select an output folder. Then all images (later also videos) that contain that person should be copied to the output folder. If an image isn't available, then the thumbnail should be copied to the output folder instead.

3. Implement video import: The app should also support tagging facces in videos. For that we will use ffmpeg4j to extract frames from the video. We will not extract all frames. Instead we limit frame extraction in the following way: not more than one frame per second; not more than 120 frames per video; the extracted frames should have the same "distance" to each other regarding time. We will then run face detection on those frames. Thumbnails of the frames and faces will be stored in the database, but referencing the video file as the source. The extracted frames can be deleted after that. See how ffmpeg4j works with the platform dependent GitHub actions.
   Progress: VIDEO_SUPPORT.md Phase 0 done - ffmpeg4j pinned (30d9a734), CI install step added, dependency + per-platform shade filters in pom.xml, pre-built Windows jar verified (105.5 MB; only windows-x86_64 ffmpeg natives bundled). Phase 1 done - videos/video_paths/video_frames schema + indexes in Database.initializeSchema(), VideoDao (10 methods) + VideoRecord/VideoFrameLinkRecord models, VideoDaoTest green. Phase 2 in progress - frame-budget math done (FrameSampler with MAX_FRAMES_PER_VIDEO=120 / MIN_FRAME_MS_SPACING=1000, VideoFrameSamplerTest green); RGB24->BufferedImage conversion done (VideoFrameUtils.rgb24ToImage, wrong-size input rejected) + SHA-256 over raw bytes for frame hashing (HashUtils.hashBytes), both tested. Test seam defined: VideoFrameSource interface (getDuration / forward-only seekTo / SampledFrame / AutoCloseable) in service. FakeVideoFrameSource (in-memory test double with configurable duration + frame positions, forward-only rejection, graceful EOF) + VideoFrameSourceTest (sampler targets -> one ascending frame each, frame+position delivery, rewind -> IllegalStateException, EOF -> null). Real ffmpeg source done: FfmpegVideoFrameSource (extension-mapped demuxer, video substream pick, container-duration probe + packet-scan fallback, monotonic forward decode capturing the first frame at/after each target, AV_PIX_FMT_NONE guard); manual smoke test on the bundled sample: 30/30 sampled frames extracted ascending, truncated sample fails cleanly with IOException. Phase 2 done - frame budget, RGB24 conversion + byte hashing, VideoFrameSource seam + FakeVideoFrameSource contract tests, and the real FfmpegVideoFrameSource all landed green with docs. Phase 3 in progress - shared face-detection pipeline extracted from ImportService.processFile into package-internal FaceDetectionUtils (photos and video frames analysed with identical criteria); ImportService refactored onto the helper, ImportServiceTest still green. Phase 0 item 6 needs a CI push to verify. Phases 3-6 remain otherwise. [Phase 1 single-commit reconciliation: test + schema + DAO + docs landed together because AGENTS.md forbids committing red code]
