1. ✅ In the "Add faces to a name" tab, after selecting a name from the list, we see thumbnails of the "Most similar unnamed faces". When right-clicking on one of them, we currently get the following two options: "Open Original" and "Paste Path to Path Filter". There I'd like a third option: "Tag with a different name". Choosing this option should open a new (dialog) frame in which the user can first see the selected face thumbnail, a text field where the user can input a name, and two buttons (ok, and cancel). The ok button should only be enabled when the text field is not empty. When typing in a name, the user should get either the hint "new name" if it's a new name. If it's an existing name, then the user should get presented with the face associated with that name (the one closest to the average embedding) and the similarity between the selected face and the average embedding of the name.

2. Whenever a context menu offers the option "Open Original" there should also be the option to open the containing folder.

--------------------

For each of the items above, do the following:
- implement and check if maven builds and tests pass
- update this file, the CHANGELOG.md, and if necessary the READMEs
- commit the change

After these steps, procees with the next item above until you are done.